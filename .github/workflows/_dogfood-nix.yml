name: '❄️ nix (dogfood)'
on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - main
permissions:
  contents: read
  id-token: write
  packages: write
jobs:
  nix:
    uses: jmmaloney4/sector7/.github/workflows/nix.yml@ce99bdfd965f2940429d77de8812854a60ae95b7 # main
    with:
      repository: ${{ github.repository }}
      ref: ${{ github.ref }}
