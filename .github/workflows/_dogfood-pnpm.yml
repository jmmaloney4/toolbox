name: '📦 pnpm'
on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - main
    tags:
      - 'v*'
permissions:
  contents: read
  packages: write
jobs:
  pnpm:
    uses: jmmaloney4/toolbox/.github/workflows/pnpm.yml@main
    with:
      repository: ${{ github.repository }}
      ref: ${{ github.ref }}
      dry_run: ${{ github.event_name == 'pull_request' || !startsWith(github.ref, 'refs/tags/v') }}
